<div class="indexPage">
    <h2>Главная страница навигации</h2>

    <ul class="index__ul">
        {{#each this.pages}}
            <li>
                <a href="{{link}}">{{title}}</a>
            </li>
        {{/each}}
    </ul>
</div>

